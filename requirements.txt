# 运行依赖
# llmclient 是独立项目，按 tag 钉住。也可用 pyproject.toml 安装：
#   pip install -e .
PySide6>=6.5
requests>=2.31
llmclient @ git+https://github.com/383827453-max/llmclient.git@v0.1.0

# 开发与打包（可选）
pytest>=7.4
pyinstaller>=6.0
